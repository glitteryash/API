<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= djs.name %> (<%= djs.sys.country %>)</title>
    <link rel="stylesheet" href="/styles/style.css" />
  </head>
  <body>
    <h1><%= djs.name %>'s Weather Now</h1>
    <table>
      <tr>
        <td>Main Weather</td>
        <td><%= djs.weather[0].main %></td>
      </tr>
      <tr>
        <td>Weather Description</td>
        <td>
          <%=
          djs.weather[0].description.charAt(0).toUpperCase()+djs.weather[0].description.slice(1)
          %>
        </td>
      </tr>
      <tr>
        <td>Temparature</td>
        <td><%= newTemp %></td>
      </tr>
    </table>
  </body>
</html>
